spring:
  datasource:
    url: jdbc:h2:mem:scrap_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  mail:
    host: localhost
    port: 2525
    username:
    password:

app:
  frontend:
    base-url: http://localhost:3000
  cors:
    allowed-origins: http://localhost:3000

jwt:
  secret: test-only-jwt-secret-that-is-long-enough-for-hs512-signatures-0000000000000000
  expiration: 86400000
