server:
  address: 0.0.0.0   # Allow connections from other devices

spring:
  application:
    name: scrap-detection

  datasource:
    url: jdbc:mysql://localhost:3306/pos_db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Tranthanh01@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update        # Options: none, validate, update, create, create-drop
    show-sql: true            # Set to false in production
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  springdoc:
    swagger-ui:
      path: /swagger-ui.html
      enabled: true